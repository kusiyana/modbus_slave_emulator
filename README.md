# modbus_slave_emulator

Install socat first:

Mac: brew install socat
Ubuntu:  sudo apt-get install -y socat

Then, when that's done:

pip install -r requirements.txt

Usage:


python rtu_slave_emulator.py


